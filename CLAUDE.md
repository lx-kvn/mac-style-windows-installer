# CLAUDE.md

## 版本 tag（`v*`）只能透過 `/released` skill 產生

**絕對不要**在沒有明確執行 `/released` 這個 skill 的情況下，自己打
`git tag v<版本號>` 或 push 任何 `v*` 開頭的 tag——即使使用者只是叫你
「打包」、「編一份 exe 出來測試」、「重新編一次安裝檔」之類的請求，那些
都只是本機測試用的建置動作，不代表使用者要發布新版本。

`v*` tag 是這個 repo 判斷「這是一次正式發布」的依據（未來 GitHub Actions
的自動建置流程也會用 `v*` tag 當觸發條件），打錯 tag 會造成版本紀錄混亂、
甚至誤觸發不該發生的自動化流程。

只有當使用者明確要求執行 `/released`（或明確說要發布新版本、要建立
GitHub Release）時，才照那個 skill 裡的完整流程（版本號確認、測試、
編譯、打包、Release Notes、commit、tag、push、建立 GitHub Release，
每一步都有明確的使用者確認關卡）走到打 tag 這一步。單純的「打包來測試」
請求，只做打包，不要順便打 tag、不要 push。

## commit 前要先問過

**預設每次 commit 之前都要先問過使用者，不要做完就自己 commit。**

例外只有一種：使用者明確給出範圍授權（例如「這一輪的修正你可以邊做邊
commit，不用一直問」）。這種授權**帶著範圍，也隨著那個範圍結束而失效**
——交代的任務做完之後就回到「要先問過」的模式，不會延續到下一件工作。
Agent 也可以主動提出範圍授權的請求，但必須把範圍講清楚（哪幾項工作、
到什麼程度算結束），不能要一個沒有邊界的授權。

另外，**判斷目前的狀態特別適合 commit 時要主動提出來**（例如一組修改
已經自成一個完整的單位、測試全綠、工作目錄乾淨），不要等使用者想到才問。

**Why：** commit 的切分方式決定了往後版本紀錄的可讀性與可回溯性，而
「哪些改動算同一件事」是使用者的判斷，不是 Agent 的。自己決定切分點，
輕則把不相關的改動綁在同一個 commit 裡、事後難以單獨回退，重則在使用者
還沒看過改動內容之前就先記進版本歷史。範圍授權之所以會失效，是因為當初
授權的理由是「這一輪不想被一直打斷」，那個理由在那一輪結束後就不存在了，
把它當成長期授權等於擅自擴大使用者給過的許可。

## 寫新功能/改架構時，先寫測試再寫實作（TDD）

**先寫測試，讓它照預期失敗，再寫實作讓它通過**——不要先寫實作、事後才
補測試或把舊測試改到能通過為止。

**Why：** 先寫實作再補測試，測試很容易在不知不覺間被寫成「描述這段
程式碼實際做了什麼」，而不是「描述這段程式碼應該做什麼」——一旦實作
中途走偏（例如重構時漏改一處、抄錯一個變數），測試也會跟著錯誤的實作
一起被寫/改成看起來綠燈，等於沒有真正驗證到當初規劃的行為。先寫測試，
測試內容綁定的是規劃階段就定好的行為定義，實作只能被動去符合它，不會
反過來被實作牽著走。

**How to apply：** 不管是新增功能、抽出新模組（例如這個 session 做的
`install_scope.py`/`self_delete.py` 那種重構），或修 bug，都先把預期
行為寫成測試（測試應該先跑起來、確認真的是失敗/紅燈，不是因為寫錯測試
本身而意外通過），再動手寫或修改實作，讓測試轉綠。跑一次全套測試套件
確認沒有波及其他地方，這一步仍然要做，但不能取代「先寫測試」這個順序。

## UI 變更注意事項

標題列、彈窗、對話框的排版每次改動後：(1) 檢查新增的控制項有沒有意外繼承到全域規則（例如 `width: 100%`）、(2) 互動控制項（下拉選單、按鈕）不能放進視窗拖曳區內、(3) 確認在支援的最窄視窗寬度下文字不會被截斷、(4) 截圖或描述排版結果後再回報完成，不要沒看過畫面就宣告做完。驗證安裝精靈 GUI（`installer_core.py`/`uninstall.py`）畫面用 `.claude/skills/run-installer-gui`（本機 skill，截圖不會拍到螢幕上其他不相干內容）。

## 製作新功能前要先問清楚需求

當發現使用者似乎想要新增功能，且需求還不夠明確時，主動提醒使用者先跑 `/grill-with-docs` 確認需求，不要直接動手實作。

如果未問清楚需求就直接實作，Agent 大多會自己補完使用者沒說的細節，結果往往不符合使用者預期。

## 撰寫註釋、技術規格文件之規範

- 語言使用繁體中文，已知限制與待辦事項分為兩個項目，放在文件的最末尾。
- 詳盡說明使用到的規格，以及為何，若是有反直覺的決議請註記。
- 語氣使用專業、非日常口語化用法之「紀錄式」敘述法。
- 絕對不使用「刻意不...」此類語法，因為 「刻意」 這個詞在我看來是貶義的詞，改採用「不使用 X ，因為 Y 」此類較為單純敘述的語法。
- 如果待辦清單內的事項完成了則改列至「已完成之待辦」項目，而不是將待辦清單內的文字劃線表示已解決。
- **超過約 250 行、或 `##` 標題超過六個的文件要有目錄**，放在開頭第一個 `##`
  之前，列出 `##` 與 `###` 兩層。新增文件時就要一併寫；替既有文件補寫時，
  同一份文件裡的所有標題都要列進去，不要只列一半。

  **Why：** 這個專案的長文件（規格文件 1300 行、MSIX 輸出規劃 1800 行）是
  拿來查的，不是拿來從頭讀的。沒有目錄時，找一件事只能靠關鍵字搜尋——而搜
  關鍵字找不到「這份文件談過哪些主題」，只找得到「哪裡出現過這個詞」。

  **How to apply：** 錨點照 GitHub 的規則產生：轉小寫、移除 Unicode 的標點
  與符號（**全形括號、全形逗號、頓號都會被移除**）、再把空白換成連字號，且
  連續空白不合併（`靜默 / 命令列` 移除斜線後剩兩個空白，錨點是兩個連字號）。
  順序不能顛倒——先換連字號再移標點會得到不同的結果。錨點寫錯的後果是連結
  看起來可以點、點了卻跳不動，比沒有目錄更糟。
  ADR（每份約一百行、結構統一）與 `docs/releases/`（內容凍結）不加。

## 文件放哪裡（docs/ 目錄結構）

持續性文件（不是一次性的 PR 說明）一律放 `docs/`，repo 根目錄只留幾份
標準/常被外部工具直接找的檔案：`README.md`（GitHub 慣例）、`CLAUDE.md`
（Claude Code 慣例）、`CONTEXT.md`（領域詞彙表，多個 skill 預期在根目錄）、
`CLI_USAGE.md`、`COMMIT_CONVENTION.md`。

`docs/` 底下依「整個專案級別」跟「個別功能/決策級別」分開，不要混在同一層：

- `docs/規格文件.md`、`docs/使用說明書.md`——**專案級別**，涵蓋整個工具，
  不是針對單一功能。
- `docs/adr/`——**個別決策級別**，一個決定一個檔案（例如
  `0001-defer-full-install-mutation-apply-undo-restructuring.md`），記錄
  「為什麼這樣決定、不是那樣」，決策一旦定案通常不會再大改。
- `docs/proposals/`——**個別功能級別**，還沒實作、仍在研究/評估階段的
  功能規劃（例如 `MSIX輸出規劃.md`）。跟 `adr/` 的差別：`proposals/` 是
  「還沒決定要不要做」，`adr/` 是「已經決定、記錄理由」。這個資料夾只放
  還沒定案的東西，做完的要搬去 `investigations/`（見下一項）。
- `docs/investigations/`——**已經做完的調查與修正紀錄**，形狀固定是
  「重現方式／機制分析 → 方案評估 → 實際做了什麼 → 驗收」（例如
  `前端整理與缺陷修正規劃.md`、`跨模組一致性稽核與修正規劃.md`）。跟另外
  兩個資料夾的差別：`proposals/` 是還沒決定，`adr/` 放的是決定本身
  （一個決定一個檔案、篇幅精簡），而調查過程動輒四百行，塞進 ADR 會把
  ADR 的可讀性撐壞——但那些「為什麼判斷是這個成因」的推導只有這裡有，
  刪掉就沒了，所以獨立成一類。ADR 與這裡的文件互相連結：ADR 記結論，
  這裡記怎麼得到結論的。
  規劃做完並驗收之後就從 `proposals/` 搬過來，同時把「待辦清單」清空、
  對應項目移進「已完成之待辦」。
- `docs/releases/`——每次發布的 `PRE-RELEASE_NOTES_v<版本號>.md`，依版本
  存檔，屬於歷史紀錄，**內容凍結、不回頭改**（裡面提到的檔案路徑就算之後
  搬過，也不用跟著更新——那是「當時真實的樣子」的快照）。
- `docs/screenshots/`——`README.md` 引用的介面截圖。

新增文件時先判斷屬於上面哪一類，不要预設丟進根目錄或 `docs/` 最上層。
`.claude/skills/released` 這個 skill 也已經配合改成把新的
`PRE-RELEASE_NOTES_v<版本號>.md` 寫進 `docs/releases/`。


## 跑測試一律從 repo 根目錄，用 unittest

```bash
python -m unittest discover -s tests -p "test_*.py"
```

**不要 `cd tests` 之後再跑**，也不要改用別的執行器。

**Why：** 已經因此誤判過一次。另一個 session 在 `tests/` 底下執行
`discover -s .`，拿到 4 個 failure 加 1 個 error，回報說「可能是你進行中的
工作弄壞的」；同一份程式碼從 repo 根目錄跑是 1456 個全綠。成因是部分測試以
相對路徑尋找 repo 裡的檔案，工作目錄一換就對不到。這種紅燈**指不出真正的
成因**——它看起來像產品程式碼壞了，而實際上只是跑的位置不對。

另有一個相鄰的陷阱：直接指定模組名（`python -m unittest tests.test_xxx`）時，
`tests/_fakes.py` 會匯入不到，因為只有 `discover -s tests` 會把那個目錄放進
搜尋路徑。症狀是 `ModuleNotFoundError: No module named '_fakes'`。

## 換行符與跳脫字元：改檔案時的兩個陷阱

**這個 repo 的換行符是混的**，沒有統一慣例——實測 `docs/adr/` 底下 13 份文件
裡 6 份是 CRLF、7 份是 LF。**改檔案時要維持該檔案原本的樣子**，不要順手統一：
整份檔案的換行被翻掉會讓 diff 變成「每一行都改了」，真正的改動就看不見了。

作法是讀進來時記住原本是哪一種，寫回去時還原：

```python
raw = open(path, "rb").read()
crlf = b"\r\n" in raw
text = raw.decode("utf-8").replace("\r\n", "\n")
...                                   # 改內容
open(path, "wb").write((text.replace("\n", "\r\n") if crlf else text).encode("utf-8"))
```

**不要用 shell 的 heredoc 寫含有反斜線或 `\n` 的程式碼。** 這個環境的 heredoc
會把它們提前展開，即使用了 `<<'EOF'` 這種本該原樣傳遞的寫法也一樣。實測後果是
字串裡多出真正的換行字元與垂直定位字元，產生 `unterminated string literal`，
而錯誤訊息指向的行數與真正的成因無關。改檔案時把修改腳本先寫成檔案再執行，
避開 shell 那一層。

## push 完要等 CI 的結果，不要推完就當作結束

**每次 push 之後都要去看 CI 跑出什麼**，綠了才算這一段工作完成：

```bash
gh run list --limit 3
```

還在跑就等它，紅了就當場處理——不要留給下一個 session、也不要因為「本機
是綠的」就判斷那個紅燈可以忽略。

**Why：** 本機這台是繁體中文、裝了一堆只有這台才有的東西（例如 `vm_lease`
由另一個 repo 提供）、執行測試的權限也與一般使用者不同。CI 的 runner 是
英文、全新架設、只裝 `requirements.txt` 裡列的東西。兩者的差異正是這一關
存在的理由——本機綠、CI 紅是常態，不是意外。

真實發生過：v0.16.0 發布時三個測試綁到執行機器的環境（兩個匯入只有開發機
才裝的套件、一個未指定語言就斷言中文字串），本機 1654 項全綠、CI 全紅，
而發現的時候 tag 已經推上 GitHub 了。其中最糟的一條是斷言「不包含」的測試
——它在英文機器上無條件通過，紅燈都不會亮，等於那條測試從來沒有驗到東西。

**哪個 workflow 什麼時候跑**（三個的觸發條件不同，不要憑印象）：

| workflow | 觸發 | 驗什麼 |
|---|---|---|
| `tests.yml` | **push 與 PR 自動** | 測試套件，約一分鐘 |
| `build.yml` | `v*` tag 或手動 | 編得出 GUI/CLI/Setup 三顆產物嗎 |
| `test-packaging-options.yml` | 只能手動 | 打包選項實際落到系統上的效果 |

發布流程另有規定：`/released` 的步驟 8 要求在打 tag 之前先手動把後兩個跑
過一次，因為它們不會因為 push 而跑。

## CI 驗不到的事情，本機有兩台虛擬機可以驗

GitHub Actions 只有一台英文的 `windows-latest`、沒有互動桌面，而且**每次都是
全新架設的環境**。以下四類它結構上就驗不到，要用本機的虛擬機
（`.claude/skills/run-test-vm`——Windows 10 1809 Enterprise LTSC 2019，以及
Windows 11 25H2 繁體中文）：

- **舊版 Windows 的行為**——MSIX 的 MinVersion 能不能部署、企業版側載預設是
  開還是關、缺少 WebView2 Runtime 時安裝精靈長什麼樣。
- **中文環境**——runner 是英文的，中文介面從來沒有在真的中文系統上跑過；
  系統地區編碼（cp950）造成的問題也只有在那裡才會現形。
- **權限與 UAC**——runner 的行程本來就是已提升的，因此「一般使用者從桌面
  啟動會怎樣」在那裡永遠測不出來。虛擬機的使用者桌面是未提升的真實情境。
- **打包機器缺了什麼**——CI 每次都明確裝好所有相依套件，因此「本機少裝一個
  套件會怎樣」它一定測不到。

**Why：** 已經因此漏掉過一次真實缺陷：打包機器沒有安裝 `winrt-*` 綁定套件
時，工具回報編譯成功、產出一顆在任何機器上都裝不起來的安裝檔，而 CI 全綠
——因為 CI 每次都明確安裝了那五個套件。錯誤一路走到終端使用者手上才出現
（完整經過見 `docs/investigations/MSIX綁定套件缺失的打包階段攔截.md`）。
**CI 綠燈不代表本機編出來的東西可用**，兩者回答的是不同的問題：CI 回答
「這份程式碼在一台乾淨機器上跑得起來嗎」，虛擬機回答「在真實的使用者機器上
會發生什麼」。

**How to apply：** 改動只要碰到上面四類其中之一，在宣告完成之前先問一次
「這件事 CI 驗得到嗎」；驗不到就跑一次虛擬機，不要用「CI 過了」代替它。
那個 skill 有三種執行模式（背景執行／使用者桌面／交給使用者親手操作），
差別在**權限**與**誰動手**，不在畫面看不看得到——選錯模式量到的會是另一個
情境的結果，而且通常不會報錯。細節看那份 skill，不要憑印象。
