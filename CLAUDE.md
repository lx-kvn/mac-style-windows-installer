# CLAUDE.md

## 版本 tag（`v*`）只能透過 `/released` skill 產生

**絕對不要**在沒有明確執行 `/released` 這個 skill 的情況下，自己打
`git tag v<版本號>` 或 push 任何 `v*` 開頭的 tag——即使使用者只是叫你
「打包」、「編一份 exe 出來測試」、「重新編一次安裝檔」之類的請求，那些
都只是本機測試用的建置動作，不代表使用者要發布新版本。

`v*` tag 是這個 repo 判斷「這是一次正式發布」的依據（未來 GitHub Actions
的自動建置流程也會用 `v*` tag 當觸發條件），打錯 tag 會造成版本紀錄混亂、
甚至誤觸發不該發生的自動化流程。

只有當使用者明確要求執行 `/released`（或明確說要發布新版本、要建立
GitHub Release）時，才照那個 skill 裡的完整流程（版本號確認、測試、
編譯、打包、Release Notes、commit、tag、push、建立 GitHub Release，
每一步都有明確的使用者確認關卡）走到打 tag 這一步。單純的「打包來測試」
請求，只做打包，不要順便打 tag、不要 push。

## 寫新功能/改架構時，先寫測試再寫實作（TDD）

**先寫測試，讓它照預期失敗，再寫實作讓它通過**——不要先寫實作、事後才
補測試或把舊測試改到能通過為止。

**Why：** 先寫實作再補測試，測試很容易在不知不覺間被寫成「描述這段
程式碼實際做了什麼」，而不是「描述這段程式碼應該做什麼」——一旦實作
中途走偏（例如重構時漏改一處、抄錯一個變數），測試也會跟著錯誤的實作
一起被寫/改成看起來綠燈，等於沒有真正驗證到當初規劃的行為。先寫測試，
測試內容綁定的是規劃階段就定好的行為定義，實作只能被動去符合它，不會
反過來被實作牽著走。

**How to apply：** 不管是新增功能、抽出新模組（例如這個 session 做的
`install_scope.py`/`self_delete.py` 那種重構），或修 bug，都先把預期
行為寫成測試（測試應該先跑起來、確認真的是失敗/紅燈，不是因為寫錯測試
本身而意外通過），再動手寫或修改實作，讓測試轉綠。跑一次全套測試套件
確認沒有波及其他地方，這一步仍然要做，但不能取代「先寫測試」這個順序。

## UI 變更注意事項

標題列、彈窗、對話框的排版每次改動後：(1) 檢查新增的控制項有沒有意外繼承到全域規則（例如 `width: 100%`）、(2) 互動控制項（下拉選單、按鈕）不能放進視窗拖曳區內、(3) 確認在支援的最窄視窗寬度下文字不會被截斷、(4) 截圖或描述排版結果後再回報完成，不要沒看過畫面就宣告做完。驗證安裝精靈 GUI（`installer_core.py`/`uninstall.py`）畫面用 `.claude/skills/run-installer-gui`（本機 skill，截圖不會拍到螢幕上其他不相干內容）。

## 製作新功能前要先問清楚需求

當發現使用者似乎想要新增功能，且需求還不夠明確時，主動提醒使用者先跑 `/grill-with-docs` 確認需求，不要直接動手實作。

如果未問清楚需求就直接實作，Agent 大多會自己補完使用者沒說的細節，結果往往不符合使用者預期。

## 撰寫註釋、技術規格文件之規範

- 語言使用繁體中文，已知限制與待辦事項分為兩個項目，放在文件的最末尾。
- 詳盡說明使用到的規格，以及為何，若是有反直覺的決議請註記。
- 語氣使用專業、非日常口語化用法之「紀錄式」敘述法。
- 絕對不使用「刻意不...」此類語法，因為 「刻意」 這個詞在我看來是貶義的詞，改採用「不使用 X ，因為 Y 」此類較為單純敘述的語法。
- 如果待辦清單內的事項完成了則改列至「已完成之待辦」項目，而不是將待辦清單內的文字劃線表示已解決。

## 文件放哪裡（docs/ 目錄結構）

持續性文件（不是一次性的 PR 說明）一律放 `docs/`，repo 根目錄只留幾份
標準/常被外部工具直接找的檔案：`README.md`（GitHub 慣例）、`CLAUDE.md`
（Claude Code 慣例）、`CONTEXT.md`（領域詞彙表，多個 skill 預期在根目錄）、
`CLI_USAGE.md`、`COMMIT_CONVENTION.md`。

`docs/` 底下依「整個專案級別」跟「個別功能/決策級別」分開，不要混在同一層：

- `docs/規格文件.md`、`docs/使用說明書.md`——**專案級別**，涵蓋整個工具，
  不是針對單一功能。
- `docs/adr/`——**個別決策級別**，一個決定一個檔案（例如
  `0001-defer-full-install-mutation-apply-undo-restructuring.md`），記錄
  「為什麼這樣決定、不是那樣」，決策一旦定案通常不會再大改。
- `docs/proposals/`——**個別功能級別**，還沒實作、仍在研究/評估階段的
  功能規劃（例如 `MSIX輸出規劃.md`）。跟 `adr/` 的差別：`proposals/` 是
  「還沒決定要不要做」，`adr/` 是「已經決定、記錄理由」。功能一旦真的
  拍板實作，對應的 proposal 文件可以整併進 `規格文件.md` 或補一份
  `adr/`，不需要一直留在 `proposals/`。
- `docs/releases/`——每次發布的 `PRE-RELEASE_NOTES_v<版本號>.md`，依版本
  存檔，屬於歷史紀錄，**內容凍結、不回頭改**（裡面提到的檔案路徑就算之後
  搬過，也不用跟著更新——那是「當時真實的樣子」的快照）。
- `docs/screenshots/`——`README.md` 引用的介面截圖。

新增文件時先判斷屬於上面哪一類，不要预設丟進根目錄或 `docs/` 最上層。
`.claude/skills/released` 這個 skill 也已經配合改成把新的
`PRE-RELEASE_NOTES_v<版本號>.md` 寫進 `docs/releases/`。