"""開發與驗證用的輔助工具，不隨產品一起打包。

與 repo 根目錄的模組分開放，理由是根目錄的模組會被 builder.py 收進產出的
exe，而這裡的東西只在開發機上跑；混在同一層時，日後很難一眼看出哪些檔案
是使用者真的會拿到的。

不放進 `.github/scripts/`，因為那裡的腳本以 GitHub Actions 為執行環境，
而這裡的工具需要本機的虛擬機，永遠不會在 CI 上執行。
"""
