# Python
__pycache__/
*.pyc
*.pyo

# PyInstaller 編譯產物（build_config_tool.py / builder.py 呼叫 pyinstaller 時產生）
build/
dist/
*.spec

# /released skill 打包成品的暫存輸出資料夾（GUI/CLI exe + 安裝檔），發布產物不進版控
release_output/

# 測試用的建置產物。跟 release_output/ 分開放：那個資料夾只放真正發布過的
# 版本，測試建置混進去會讓人分不出哪些是真的發布過的。測試建置的檔名不用
# 版本號形式（用日期），版本欄位固定是「目前版本-dev」，不編造不存在的版本號。
test_output/

# Claude Code 本機設定/skill，不進版控

# .claude 只收這個專案自己的 skill。
#
# 這些 skill 是專案的一部分：CLAUDE.md 直接指名它們，而 run-test-vm 的
# REFERENCE.md 收錄了十三項實測出來的陷阱與兩台虛擬機的環境事實——那些
# 內容在別處沒有第二份，不進版就只存在於單一一台機器上。
#
# 其餘一律排除：底下大部分是第三方 skill（見 skills/THIRD_PARTY_SKILLS.md），
# 收進來會讓版本紀錄充滿與本專案無關的內容；worktrees 是 agent 的工作副本，
# 實測 194 MB；settings.local.json 與 scheduled_tasks.lock 是本機狀態。
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/run-test-vm/
!.claude/skills/run-installer-gui/
!.claude/skills/released/
!.claude/skills/test-packaging-options/
!.claude/skills/grill-with-docs/
