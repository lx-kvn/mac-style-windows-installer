# 驗證覆蓋率的稽核與補強（2026-09-09～12）

- [起因](#起因)
- [稽核當時的覆蓋範圍](#稽核當時的覆蓋範圍)
- [補強的七項](#補強的七項)
  - [一 更新覆蓋安裝](#一-更新覆蓋安裝)
  - [二 安裝密碼保護](#二-安裝密碼保護)
  - [三 安裝失敗時的回滾](#三-安裝失敗時的回滾)
  - [四 授權合約頁](#四-授權合約頁)
  - [五 捷徑](#五-捷徑)
  - [六 檔案關聯](#六-檔案關聯)
  - [七 相依元件](#七-相依元件)
- [五個從來沒有驗到東西的檢查](#五個從來沒有驗到東西的檢查)
- [判準要有辦法失敗](#判準要有辦法失敗)
- [第二輪補強](#第二輪補強)
  - [新增的六支工具](#新增的六支工具)
  - [抓到的兩個產品缺陷](#抓到的兩個產品缺陷)
  - [三次看起來有結論其實沒驗到](#三次看起來有結論其實沒驗到)
  - [畫面上的東西怎麼問、按鈕怎麼按](#畫面上的東西怎麼問按鈕怎麼按)
- [第三輪補強：配置精靈](#第三輪補強配置精靈)
  - [先把環境裝進去](#先把環境裝進去)
  - [六輪才走通的那一段](#六輪才走通的那一段)
  - [抓到的第三個產品缺陷](#抓到的第三個產品缺陷)
  - [三條自己寫出來的假綠燈](#三條自己寫出來的假綠燈)
  - [原生對話框的操作方式](#原生對話框的操作方式)
- [現況對照表](#現況對照表)
- [已知限制](#已知限制)
- [待辦事項](#待辦事項)
- [已完成之待辦](#已完成之待辦)

## 起因

v0.17.0 發布過程中，使用者問了一個問題：「虛擬機跟 CI 驗證會檢查這個專案實現
的所有功能嗎？」

答案是不會，而且差距比預期大。本文記錄那次稽核的結果、依此補強的七項，以及
補強過程中反覆出現的一種缺陷——**看起來一直是綠燈、實際上什麼都沒有驗到的
檢查**。後者是這次最值得留下的部分：五個之中有一個是既有的，掛在那裡不知道
多久。

## 稽核當時的覆蓋範圍

把「有沒有自動測到」定義為**有沒有真的裝在一台機器上、然後去看系統狀態**，
不計單元測試——那 1925 項測的是程式碼，不是編出來的東西。

三道關卡各自回答不同的問題：

| | 回答什麼 |
| --- | --- |
| `build.yml` | 這份程式碼在一台乾淨的英文機器上編得出三顆產物嗎 |
| `test-packaging-options.yml` | 打包選項真的落到系統上了嗎、移得乾淨嗎 |
| `tools/verify_release_build.py` | 這一顆發布產物在真實機器上裝得起來、跑得動、移得乾淨嗎 |

稽核當時 `test-packaging-options.yml` 一共只斷言 16 條，涵蓋 30 個打包欄位裡
的 20 個。**完全沒有被碰過的有 10 個**：`doc_icon`／`doc_icons`、`folder_name`、
`eula_texts`／`eula_default_lang`、相依元件那一整組四個、`local_appdata_files`、
`install_password_env`。

而更大的空缺是整塊功能沒有自動化：互動安裝的畫面本身、UAC 提權那條路、升級
覆蓋安裝、解除安裝的拖曳、安裝密碼保護的流程、英文系統上的介面顯示。

排序的標準是**失敗有多安靜**，其次是後果可不可逆。看得見的失敗排後面——那種
自己裝一次就會發現。

## 補強的七項

### 一 更新覆蓋安裝

這是這個工具**唯一會刪除使用者既有檔案**的路徑：備份舊資料夾 → 呼叫舊版的
解除安裝程式移除 → 裝新版 → 中途失敗時把備份復原。`upgrade.py` 記著三輪真實
抓到的 bug，其中兩輪的症狀不是當場報錯：「時好時壞」與「回報成功但檔案沒有
複製完整」。

**CI 結構上做不到這一關。** `upgrade.py` 對「舊版的登錄表項目在 HKCU、而目前
行程已提權」的組合有一道安全防護會直接拒絕代為執行（來源不受信任 + 已提權 =
任意程式碼執行），而 runner 永遠是提權的。HKCU 正是這個專案自己的安裝檔所用
的設定（`no_admin_install=true`）。

因此改由 `tools/verify_upgrade_install.py` 在虛擬機的**未提權桌面**上驗，
三輪：裝舊版 → 裝新版 → 解除安裝。驗的是磁碟與登錄表上的結果，不是安裝程式
自己回報的成敗。

### 二 安裝密碼保護

加密沒有生效時，安裝檔照樣編得出來、照樣裝得起來、行為看起來一模一樣，只是
保護不存在。這個欄位在此之前沒有被任何自動化碰過。

`test-install-password` job 驗七件事，其中三件關於「內容真的被加密了」——驗的
是**內嵌資源的名字**而不是搜尋明文內容，理由見下一節。

### 三 安裝失敗時的回滾

安裝的順序是複製檔案 → 登錄表 → 捷徑 → 檔案關聯 → PATH → 寫
`install_manifest.json`，而回滾只由檔案系統錯誤觸發（後置腳本失敗只算警告，
不能拿來當觸發點）。

觸發的作法是**事先在安裝目錄放一個名為 `install_manifest.json` 的資料夾**，
最後那一步就會拋出 `OSError`，而前面每一樣都已經寫進去了——正好是回滾要清的
全部。結束碼若為 0 就直接中止並說明「這一輪沒有觸發到回滾，後面的檢查沒有
意義」，不讓它變成一次空的通過。

另有一項反向的斷言：安裝目錄裡事先放一個不屬於這次安裝的檔案，回滾之後它
必須還在。回滾清太多與清太少一樣是缺陷，而清太多不會有人抱怨——東西默默
消失了而已。

### 四 授權合約頁

合約頁只出現在**互動安裝**：靜默安裝的既定行為就是跳過它（`/S` 視同已經
同意）。`/released` 的步驟 6 警告過「忘了帶 `--config` 時 `eula_texts` 為空，
安裝檔會安靜地跳過那一頁、不報錯」，卻同時**錯誤地宣稱步驟 8 驗得到它**
——步驟 8 做的正是靜默安裝。該處已修正並新增步驟 8c。

`tools/verify_eula_gate.py` 以「那一頁擋不擋得住」為判準，在客體桌面上送真正
的滑鼠事件，三輪：

| 輪次 | 材料 | 動作 | 預期 |
| --- | --- | --- | --- |
| 對照組 | 沒有合約的安裝檔 | 直接拖曳 | 裝起來 |
| 擋住 | 有合約的安裝檔 | 直接拖曳 | **沒有**裝起來 |
| 同意 | 有合約的安裝檔 | 先按「同意並繼續」再拖曳 | 裝起來 |

第三輪不能省：合約頁壞掉、按鈕點不動時，第二輪一樣會通過（檔案確實沒落地，
只是原因完全不同）。

按鈕的位置以**視窗矩形的比例**表示，與既有的 `ICON_AT`／`TARGET_AT` 同一套。
量的時候要注意截圖是實體像素而 `GetWindowRect` 是邏輯像素，該機器縮放 125%、
兩者差 1.25 倍。用比例而非絕對座標另有實證的好處：實測中有一輪視窗開在
`(51,51)` 而不是 `(26,26)`，仍然點得準。

### 五 捷徑

桌面與開始功能表的捷徑在此之前**任何地方都沒有被斷言過**——CI 沒有、虛擬機
也沒有——而那是使用者裝完之後第一個會去找的東西。產品裡捷徑建立失敗是吞掉
例外、記一則警告，安裝照樣回報成功，因此「沒建立」不會有任何徵兆。

與回滾寫在同一個 job：正常安裝那一輪本身就是捷徑的涵蓋，同時當回滾那一輪的
對照組。

### 六 檔案關聯

原本的檢查在 `HKLM` 底下找，而 `file_assoc.py` 早就接上了 `InstallScope`：
`no_admin_install=true` 時寫的是 `HKCU\Software\Classes`。也就是說它一直在
**錯的地方找**，於是永遠 WARN、永遠不會失敗——而原本的註解把原因歸給
「runner 的 UAC/權杖狀態」，那個診斷也是錯的。

修正之外把斷言加深到真正讓「雙擊那個副檔名會開啟這支程式」成立的那條鏈：
副檔名指向 ProgID、ProgID 底下的開啟命令指向裝好的那支 exe。少了後者的話，
關聯存在但點了沒反應。

### 七 相依元件

`dependencies`／`custom_dependencies`／`bundle_dependencies` 三個欄位完全
沒有被碰過。偵測沒接上時安裝照樣成功，只是那則「建議先安裝」永遠不出現；
內嵌沒生效時安裝檔照樣編得出來，只是它在離線的機器上會去下載一個下載不到
的東西。

偵測那一段用**同一顆安裝檔跑兩次，中間只差一個登錄表機碼**：機碼在 → 不該
提示；機碼不在 → 必須提示，而且要帶著下載連結。同一顆安裝檔、同一個指令，
輸出卻不同，那是「偵測真的有在跑」唯一不靠推論的證據。偵測指向的是這個 job
自己建立的機碼，因此結果不受 runner 上實際裝了什麼影響，也不需要下載任何
東西。

內嵌那一段比對兩顆安裝檔的內嵌資源名稱，另外比對檔案大小差距（實測
48,874,022 對 67,352,435）。兩個訊號指向同一件事，其中一個壞掉另一個還在。

## 五個從來沒有驗到東西的檢查

補強過程中一共抓到五個，其中四個是這次自己寫的、一個是既有的。它們的共同
形狀是：**斷言永遠成立，因此永遠是綠的，而它什麼都沒有驗到。**

1. **備份殘留的搜尋條件寫錯。** 條件要求資料夾名稱同時含 `backup` 與應用程式
   名稱，而實際的備份叫 `mswi_upgrade_backup_<pid>`，裡面沒有應用程式名稱。
   那個條件永遠不成立。第一次跑就兩項全綠，去對了實作才發現。
2. **想用明文標記驗加密。** 打算在 `app_dir` 放一串字再去 exe 的位元組裡找
   它——先拿一顆**未加密**的探針試過才知道行不通：PyInstaller 的 onefile
   封存會壓縮每一筆內容，就算完全沒有加密，明文也不會以原樣出現。照那樣寫
   的話對照組會失敗，整個檢查什麼都證明不了。改為驗**內嵌資源的名字**
   （未壓縮儲存，而且是 `embedded_payload` 模組說明明文記載的契約）。
3. **檔案關聯在錯的 hive 找。** 見上一節。**這是既有的**，掛在那裡不知道
   多久，而且註解裡的診斷是錯的。
4. **相依元件的「不該出現提示」單看是空的。** 偵測根本沒接上時它也會通過，
   而這次正是如此——第一輪綠、第二輪紅。如果當初只寫了第一輪，整個 job 會
   綠燈通過，而相依元件偵測其實完全沒被驗到。
5. **兩顆安裝檔的檢查，第二次打包把第一顆砍了。** `builder_cli.py pack` 底層
   的 `build_all()` 開頭會清空整個 `dist/`——這個坑 `.claude/skills/released`
   的步驟 6 就明文記著，是 v0.11.0 踩過的同一件事。這一個的症狀是「找不到
   檔案」，比前四個好發現，但它同樣讓對照組失去意義。

## 判準要有辦法失敗

從上面五件事歸納出來的作法：**每一條斷言都要有證據證明它能失敗**，三種形式，
擇一即可。

**一、同一個表達式在前後兩次量出不同的值。** 更新覆蓋安裝那一輪，登錄表項目
數與 PATH 出現次數在解除安裝之後用同一組函式再量一次——升級後是 1、移除後是
0。兩次同一個數字的話，那個數字有可能只是這段程式碼永遠回傳的東西。

**二、誘餌。** 備份殘留的搜尋先在一個刻意建立的資料夾上跑一次，抓不到誘餌時
判為**無法判定**而不是通過——那時候「沒有殘留」與「搜尋壞了」在結果上一模
一樣。用完立刻刪掉，否則誘餌自己會變成下一輪的殘留。

**三、對照組。** 密碼保護、相依元件內嵌、合約頁三處都用了：一份**應該找得到**
的材料先跑同一段檢查。對照組沒過時，另一邊的「找不到」不列為通過。

還有一條不屬於這三種、但同樣重要：**刻意觸發的失敗要確認真的失敗了**。回滾
那一輪若安裝回報成功就直接中止並說明「這一輪沒有觸發到回滾」——不然後面每
一條「東西不見了」都會通過，因為那些東西本來就沒被寫進去過。

## 第二輪補強

第一輪之後，使用者要求把剩下的清單依重要程度做完。第三級（打包欄位與命令列
旗標）併進 `test-packaging-options.yml`，job 從六個長到九個；第四級（互動畫面）
需要在真實桌面上操作，全部落在虛擬機這一側。

### 新增的六支工具

| 工具 | 幾輪 | 驗什麼 | 結果 |
| --- | --- | --- | --- |
| `verify_uninstall_drag` | 2 | 把圖示拖到垃圾桶會移除；完成畫面按下「完成」之後整個目錄才消失 | 通過 |
| `verify_install_password` | 4 | 密碼那一關會出現、擋得住、打錯被拒、打對放行 | 通過 |
| `verify_ui_language` | 2 | 介面語言跟著系統語言走；完成畫面也是 | **抓到缺陷** |
| `verify_eula_fallback` | 2 | 合約對不到系統語言時回退到指定的預設語言 | 通過 |
| `verify_msix_dialogs` | 4 | MSIX 降版詢問的三條路；傳統模式到 MSIX 的遷移 | **抓到缺陷** |
| `drive_installer_gui` | — | （既有工具擴充：讀畫面文字、按名字按按鈕、等頁面畫好） | — |

每一支的輪次安排都遵循同一個形狀：**一輪當對照組，證明這套機制在這台機器上
有效**；其餘各輪只差一個變因。少了對照組的話，「沒有發生」與「這台機器上什麼
都不會發生」分不出來。

### 抓到的兩個產品缺陷

**一、英文系統上完成畫面混中文。** 那一頁的標題是 `Installation Complete`，
內文卻是一句中文「安裝成功」。成因是 `installer_core` 回給畫面的 `message`
寫死中文，而前端只在它與標題一字不差時才不顯示（`messageBeyondTitle()`）——
中文系統上兩者剛好一樣，因此這個缺陷在中文機器上完全看不出來，而 CI 沒有畫面
也看不到。同一個成因下還有十幾句錯誤訊息（磁碟空間不足、程式執行中、檔案
驗證不過……）同樣寫死中文。

**二、傳統模式換 MSIX 之後留下孤兒。** MSIX 裝上了、舊版的檔案也清掉了，但
`%LOCALAPPDATA%\Programs\<App>\` 這個目錄還在，裡面剛好剩一支
`uninstall.exe`。成因是遷移時以 `--upgrade` 去跑舊的解除安裝程式，而
`self_delete.schedule_if_needed()` 對這個旗標不排背景自我刪除——那在傳統換
傳統的更新裡是對的（避免那段 `rmdir /s /q` 把剛複製進去的新檔案一併砍掉），
但傳統換 MSIX 沒有任何東西會複製回那個資料夾。使用者雙擊那支殘留的解除安裝
程式時，要移除的那一份已經不在了。

兩條判準在產品那邊處理之前都會紅——那是它們該做的事。

### 三次看起來有結論其實沒驗到

第一輪歸納的那條規則（判準要有辦法失敗）在這一輪又被觸發三次，而且三次都不是
產品的問題，是**驗證本身**寫錯：

**一、順序反了。** 更新與降版那幾個對話框是安裝檔一開起來就問的，不是拖完才
問（前端的說明：問題一定要在觸發安裝之前問完，webview 的前端沒有辦法在
Python 呼叫中途回答問題）。第一版寫成先拖再按，那一下拖在對話框的遮罩上、
什麼都沒碰到，而按完按鈕之後也沒有人再拖一次。抓到它的是「按繼續」那一輪——
少了它，「按取消版本不變」會一路綠著，而那個綠與「兩下都沒按到」長得一模
一樣。

**二、`invoked` 這條回報永遠是 True。** 用輔助使用介面的 `Invoke()` 按按鈕，
它丟例外時腳本仍會往下走，而下一行照樣把「按到了」記成 True。這是自己寫出來
的假檢查，形狀與第一輪抓到的那五個完全相同。

**三、送出去的按鍵沒有進到那個視窗。** 改成把焦點移過去再按空白鍵之後，焦點
確實移過去了（`focus_error` 是空的）、對話框卻沒關掉——`SendKeys` 送的是
**前景視窗**，而前景是跑腳本的那個主控台。密碼那一輪之所以打得進去，是因為
它在打字之前先用真滑鼠點過輸入框，順手把視窗帶到了前景。

### 畫面上的東西怎麼問、按鈕怎麼按

這一輪建立的兩個作法，後續的互動驗證都沿用：

**畫面上的文字經由輔助使用介面讀回來。** WebView2 會把網頁內容掛在那棵樹上，
因此讀到的是使用者眼睛看到的那幾個字，不是程式碼裡的常數。「介面是哪個語言」
這種問題因此量得出來，不必靠人看截圖。

**按鈕按名字叫，位置向它本人問。** 先以名稱找到那顆按鈕，再讀它的
`BoundingRectangle`，然後用與拖曳同一套滑鼠事件點它的中心。精準度來自名字
（找不到就當場回報「畫面上有哪些」，不會默默點在空白處），輸入路徑則與使用者
相同。純用座標的版本點得到，卻分不出「按到了但沒作用」與「根本沒按到那一顆」。

**視窗出現不等於那一頁畫好了。** 原本固定等三秒就開始拖，實測有一輪視窗第
13.5 秒才出現、拖的時候網頁內容還沒掛上去，結果「拖了但什麼都沒發生」，而
報告上看起來像手勢壞掉。改成等到輔助使用樹上出現指定的字串為止。

## 第三輪補強：配置精靈

2026-09-12。前兩輪之後唯一整塊未涵蓋的就是配置精靈——使用者實際填表單、按下
「開始編譯安裝檔」的那個畫面。CLI（`builder_cli.py`）走的是另一條路，CI 用的
也是那一條。

新增兩支工具：`tools/provision_build_env.py`（把編譯需要的環境離線裝進客體）
與 `tools/verify_config_wizard.py`（填表單、按編譯、檢查產物）。

### 先把環境裝進去

驗這一段有個前置條件：客體要真的編得出安裝檔。而那台機器**沒有 Python，也
沒有網路**（實測 `pypi.org` 解析不到），`pip install` 這條路不存在。作法是主機
端先把 Python 的離線安裝檔與 `requirements.txt` 每一項的 wheel 抓好，送進客體
離線安裝，實測約五分鐘（傳 47 MB 加上安裝）。

三件實測到的事：

- **`pywebview` 不能單純用 `pip download`。** 它的相依套件 `proxy_tools` 只有
  原始碼、沒有現成的 wheel，`--only-binary=:all:` 會因此一路退回到 3.4 那種舊
  版。改用 `pip wheel`，讓主機當場編出 wheel 再一起送。
- **這一段不能落在使用者桌面那個工作階段。** 那裡的行程是一般權限，Python
  安裝檔要寫 `C:\Python313` 於是彈出權限詢問窗，而那個窗按不動（見「已知
  限制」），整段停在那裡等一個沒有人會按的按鈕——主機端等了十四分鐘沒有回來，
  客體裡量到 `consent.exe` 掛在工作階段 1。改用背景模式，同一個指令量到的是
  High Mandatory Level，安裝不會被問。

  這同時修正了 `.claude/skills/run-test-vm` 原本的說法。那裡寫「客體程式拿到
  的是已提升的權限」，而那句話只在背景模式下成立，`interactive=True` 不成立。
- **裝完之後，桌面那個工作階段看不到新的 PATH。** 它是登入時定下來的。實測
  後果是配置精靈照樣跳出「缺少編譯安裝檔所需的環境」。啟動受測程式時要自己把
  Python 的目錄接到 `PATH` 前面——而這件事也正好變成一組現成的對照組，見下。

### 六輪才走通的那一段

卡住的是「選擇資料夾」那個原生對話框。它是強制回應的視窗，沒關掉後面每一步都
動不了。六輪的經過：

| 輪次 | 作法 | 結果 |
| --- | --- | --- |
| 一 | 找確認鈕、用 `Invoke` 按 | 它以 `Pane` 露出，不支援 `Invoke` |
| 二 | 拿「桌面根元素的寬 ÷ `GetSystemMetrics(0)`」當縮放倍率去點 | 倍率量到 1，點不中 |
| 三 | 移過去之後問滑鼠停在哪，對不上就修正 | 回報永遠對得上，仍然點不中 |
| 四 | 把腳本行程設成認得高解析度顯示 | 座標對了，對話框仍然沒關 |
| 五 | 窄問題：問系統那個點上是誰 | `WindowFromPoint` 回報 `Button "選擇資料夾"`，而且**這一輪關掉了** |
| 六 | 點完檢查、沒關就重試 | 穩定通過 |

**輔助使用介面說它是 `Pane`、不給 `Invoke`，但底下是一顆真的按鈕。** 這是第五
輪才問出來的，而問法是關鍵：前四輪都在拿「我送出去的座標」跟「我自己讀回來的
座標」互相印證，第五輪改問另一個來源（`WindowFromPoint`）。

**座標對不上的成因是 DPI。** 不設定的話 `GetSystemMetrics` 與 `GetCursorPos`
回報縮放過的邏輯像素（實測 2046x952），而輔助使用介面的元素矩形是實體像素
（2558x1190），點下去落在 1.25 倍遠的地方。第二、三輪都試圖「算出倍率」，而
那個推論的前提本身會變——碰過輔助使用介面之後 `GetSystemMetrics` 回報的東西
就換了。正解是把行程設成認得高解析度顯示，讓三者同一套；而「設定有沒有生效」
有個會變的數字可以看，就是那個螢幕尺寸。

第五輪與第四輪的差別只在點擊事件的送法與中間多出來的幾秒，沒有查到是哪一項
造成的。因此工具採「點完檢查、沒關就重試」，不賭任何一種說法。

### 抓到的第三個產品缺陷

**配置精靈在一台完全沒有 Python 的機器上顯示「✓ Python 直譯器」。** 原因是
`packaging_core.check_build_environment()` 只判斷 `shutil.which("python")` 找不
找得到那個名字，而 Windows 預設就把微軟商店的捷徑替身放在 `PATH` 上，名字永遠
找得到。

後果不只是顯示錯：同一頁建議的 `pip install pyinstaller pywebview pywin32` 在
那台機器上一定失敗（pip 要有 Python 才存在），而使用者看到那格是綠的，不會往
「其實沒有 Python」那個方向查。同一個對話框下面就寫著「如果連 Python 都還沒
裝，請先前往官網下載」——那句提示被上面的 ✓ 打臉。

判斷方式因此改成真的執行它一次（`print('PYTHON_OK')`，商店的替身只會印出一段
叫人去商店安裝的訊息），見 `tests/test_python_detection.py`。

### 三條自己寫出來的假綠燈

這一輪自己寫出了三條永遠成立的檢查，都在跑過之後才發現：

- **「頁面上找得到那個路徑」**。整棵樹裡包含那個還開著的對話框，而路徑正是
  自己剛打進去的。改成看**頁面上**「未選擇資料夾」那行有沒有被換掉。
- **「滑鼠停在我叫它去的地方」**。送出去的座標與滑鼠回報的座標活在同一套空間
  裡，兩者一致不代表點到了元素。改成問 `WindowFromPoint`。
- **「這支工具沒有用桌面模式跑」用搜字串判斷**。模組說明裡本來就寫著「不以
  `interactive=True` 執行這一段」，那條檢查被自己的註解騙過去。改成數語法樹
  上的呼叫，並配一個誘餌確認它數得出來。

### 原生對話框的操作方式

三個原生對話框（選資料夾、兩次選檔案）的共同點是**裡面唯一的真輸入框是搜尋
方塊**：選資料夾那個的「資料夾:」、開啟檔案那個的「檔案名稱(N):」都只以
`Pane` 露出，寫不進去。因此：

- 選資料夾走網址列：Alt+D → 路徑 → Enter，證據是「位址:」那格的名字變成
  目標路徑。
- 選檔案點標籤右邊那格（標籤本身只有 85x19，輸入框緊接在右邊）再打字。
- 確認鈕一律用點的，點完檢查、沒關就重試。

**送鍵盤之前一定要先確認前景視窗是誰。** 工具原本有一條「找不到欄位就用鍵盤
硬打」的備案，實測那次焦點在檔案清單上，路徑被當成重新命名檔案，跳出「檔案
名稱不可以包含下列任意字元」，整段停在那裡二十四分鐘。往不知道是誰的地方送
輸入就是這個故障的成因，因此改成找不到就停下來回報。

另一條同類的教訓：**打完路徑之後檔名格會跳出自動完成的候選清單，那當下整棵樹
讀不到東西**，於是「開啟」按鈕找不到，而症狀是「按鈕不見了」，看不出跟打字有
關。作法是把要用的元素都先抓在手上再動手。

## 現況對照表

三輪補強之後，`test-packaging-options.yml` 有九個 job，虛擬機工具有十四支。

**CI 涵蓋的打包欄位**：`install_engine`、`msix`、`app_dir`、`png_icon`、
`ico_icon`、`app_name`、`version`、`publisher`、`exe_name`、`main_exe`、
`no_admin_install`（兩個值都驗）、`add_to_path`、`file_associations`、
前置／後置腳本、`signing`（兩種模式）、`windows_service`、`scheduled_task`、
`create_restore_point_before_install`、`install_password_env`、
`dependencies`、`custom_dependencies`、`bundle_dependencies`。

**虛擬機涵蓋的**：發布產物的煙霧測試（`verify_release_build`）、更新覆蓋安裝
（`verify_upgrade_install`）、授權合約頁（`verify_eula_gate`）、合約的語言回退
（`verify_eula_fallback`）、拖曳手勢（`drive_installer_gui`）、解除安裝的拖曳
與完成畫面（`verify_uninstall_drag`）、安裝密碼那一關（`verify_install_password`）、
介面語言與完成畫面（`verify_ui_language`）、MSIX 的降版詢問與遷移
（`verify_msix_dialogs`）、MSIX 全機器範圍（`verify_msix_all_users`）、
使用者範圍的行為量測（`measure_msix_scope`）、1809 的最低版本與側載
（`verify_msix_1809`）、配置精靈填表單到編譯出安裝檔（`verify_config_wizard`，
搭配 `provision_build_env` 把編譯環境離線裝進客體）。

**仍未涵蓋的打包欄位**：無。第一輪列出的五個已於第二輪補完。

**仍未涵蓋的整塊**：無。配置精靈已於第三輪補完。UAC 提權那條路經實測確定無法
自動化（見「已知限制」），不列入。

## 已知限制

- **UAC 那個視窗按不到，成因不是安全桌面**（2026-09-11 實測三輪）。原本這裡
  記的是「安全桌面不接受合成輸入」，那只是一半：

  | 量到的 | 結果 |
  | --- | --- |
  | `PromptOnSecureDesktop` 設為 0 之後，提示是否回到一般桌面 | 是，列舉得到、矩形讀得到 |
  | 排程工作（最高權限 + 只在使用者登入時執行）能否給到「已提權且在使用者桌面上」 | 能 |
  | 提權的行程能否讀那個視窗的輔助使用樹 | **不能**，整棵樹是空的 |
  | 點擊的座標是否真的落在那個對話框上 | 是，`WindowFromPoint` 回傳的就是它本人 |
  | `SendInput` 有沒有收下那些事件 | 有，回傳 1 與 2 |
  | 對話框有沒有反應 | **完全沒有**，視窗仍在、被提權執行的程式沒有留下任何痕跡 |

  也就是說：事件送得出去、瞄得也準，但到不了那個視窗。擋住的是**權限層級**
  ——畫那個對話框的 `consent.exe` 以 SYSTEM 身分執行，比提權的管理員還高一級，
  而 Windows 不讓低層級的行程把輸入送給高層級的視窗。關掉安全桌面只移走了
  第一道牆。

  Windows 提供的正規出口是讓自動化程式帶 UIAccess 旗標——那需要一顆經過簽章、
  且安裝在受保護位置的執行檔。為了驗證而簽一顆這種程式不在這次的範圍內，因此
  這一項維持由人工確認。MSIX 全機器範圍那一關只驗它兩側的東西，「使用者按下
  取消」那條路徑僅由單元測試涵蓋。
- **在客體裡執行程式時，提權與否取決於落在哪個工作階段**（2026-09-12 實測）。
  背景模式量到 High Mandatory Level（已提升），`interactive=True`（使用者桌面）
  量到的是一般權限。因此需要寫系統位置的步驟要用背景模式，而需要看得見視窗、
  或會彈出原生對話框的步驟只能用桌面模式——後者一旦碰到需要提權的動作就會停在
  那個按不動的權限詢問窗上。兩者無法兼得，安排步驟時要先分清楚。
- **CI 的 runner 永遠是提權的**，因此凡是行為取決於「未提權」的路徑都驗不到。
  更新覆蓋安裝是已知的一例，其他尚未逐一檢視。
- **`create_restore_point_before_install` 在 CI 上只警告不失敗**：Server 系
  runner 上系統還原不保證存在。這一項終究要在真正的 Windows 用戶端上人工
  確認。
- 每一支工具的判準都有單元測試，**驅動虛擬機的那一段沒有**——真的跑一輪要好
  幾分鐘。因此接線層級的錯誤只有在真的跑一次時才會現形。第一輪踩到一次（把
  回傳兩個值的函式當成回傳一個，三輪跑完才在計算判定時失敗），第二輪踩到四次
  （順序反了、客體回報被拆成兩行、送出去的按鍵進不了目標視窗、客體重開機之後
  沒有人登入因此拖不動任何東西）。

## 待辦事項

1. **UAC 提權那條路確定無法自動化**（見「已知限制」那一條的實測結果），因此
   不列為待辦。要涵蓋它只剩「簽一顆帶 UIAccess 的執行檔」這條路，而那是另一
   件事。
2. **打包工具自己的兩顆 exe（GUI／CLI）沒有簽章。** 安裝檔那一端有完整的簽章
   支援（`builder.py` 的 `_certificate_arguments()`），但 `build_config_tool.py`
   整份檔案裡沒有任何簽章相關的程式碼——也就是說 GitHub Release 上掛的那兩顆
   是未簽章的。

   這不是理論上的風險：2026-09-11 在開發機上編出來的 GUI 版 exe **連續兩次在
   落地後幾十秒內被防毒（火絨）當成 `TrojanDropper/W64.Agent.ay!crit` 拿走**，
   第一次還因此誤判成「建置流程回報成功卻沒有產物」。成因是 PyInstaller 單檔
   模式的行為字面上就是 dropper 的定義（啟動時把內嵌的那一包解壓到暫存目錄再
   執行），而這顆又是一個「會產生 exe 的工具」、其產物還帶提權需求——每一項
   單獨看都是正常功能，加起來正好是啟發式規則最敏感的組合。未簽章、剛出現、
   沒有信譽再各加一分。

   下載的人會遇到同一件事，而且他們不會知道那是誤判。簽章是這類誤判唯一真正
   的解法。這一項是這次順帶發現的，不屬於驗證覆蓋率的範圍，記在這裡是因為
   發現的當下沒有更合適的地方。

## 已完成之待辦

- 把「判準要有辦法失敗」寫進 `CLAUDE.md`（2026-09-09）。五個假檢查裡有一個是
  既有的，表示這不是單次的疏忽，因此立成規則而不只是記錄。
- 把 MSIX 全機器範圍的驗證接進 `/released` 的步驟 8d（2026-09-09）。
- 補上第一輪列出的五個打包欄位（2026-09-09～11）：`doc_icon`／`doc_icons`、
  `folder_name`、`local_appdata_files`、`dependencies_min_version`、
  `eula_default_lang`。前四個併進 CI 的 job，最後一個因為要看畫面上顯示哪一份
  合約，落在虛擬機（`tools/verify_eula_fallback.py`）。
- 自訂相依元件定義了卻沒有列進 `dependencies` 時，打包階段現在會擋下來
  （2026-09-09）。原先那一筆會默默沒有作用。
- **英文系統上完成畫面混中文**（2026-09-12 修正）。做法不是改那一句，而是把
  安裝流程送到畫面上的每一個字都納入多語——`installer_core.py` 新增
  `MESSAGES` 表（29 個鍵 × zh-TW／en），二十五處寫死的字串改走 `_t()`。
  `tests/test_installer_messages_language.py` 要求兩種語言的字串不同、英文那
  張表裡不出現漢字。
- **傳統模式換 MSIX 之後留下孤兒 `uninstall.exe`**（2026-09-12 修正）。
  `installer_core.remove_legacy_install_dir()` 在遷移完成後清掉只剩殘留檔的
  舊目錄，清不掉時由 `msix_install` 以 `legacy_dir_left` 這個訊息鍵回報，不
  默默略過。自我刪除對 `--upgrade` 的既有判斷沒有動——它在傳統換傳統的路徑
  上是對的。
- **配置精靈的 GUI**（2026-09-12 補上）。`tools/verify_config_wizard.py` 從
  空白桌面開始一路做到產出安裝檔，`tools/provision_build_env.py` 負責把編譯
  環境離線裝進客體。經過見「第三輪補強」。
